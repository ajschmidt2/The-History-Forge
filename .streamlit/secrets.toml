openai_api_key = "PASTE_KEY_HERE"
GEMINI_API_KEY = "PASTE_KEY_HERE"
elevenlabs_api_key = "PASTE_KEY_HERE"

# Supabase credentials — see SUPABASE_SETUP.md for setup instructions
SUPABASE_URL = "https://xxxxxxxxxxxx.supabase.co"
SUPABASE_KEY = "your-anon-key-here"

# ---------------------------------------------------------------------------
# AI Video Generation — Google Veo via Vertex AI
# ---------------------------------------------------------------------------
# Your Google Cloud project ID (e.g. "my-gcp-project-123")
GOOGLE_CLOUD_PROJECT_ID = "PASTE_PROJECT_ID_HERE"

# Vertex AI region where Veo is available (us-central1 is the default)
GOOGLE_CLOUD_LOCATION = "us-central1"

# Short-lived OAuth 2.0 access token for Vertex AI.
# Generate with: gcloud auth print-access-token
# WARNING: In production this should be fetched server-side via a service
# account — do NOT hard-code a long-lived credential here.
GOOGLE_ACCESS_TOKEN = "PASTE_ACCESS_TOKEN_HERE"

# ---------------------------------------------------------------------------
# AI Video Generation — OpenAI Sora
# ---------------------------------------------------------------------------
# The openai_api_key above is reused automatically.  No extra key needed.
