openai_api_key = ""
openai_model = "gpt-5-mini"
GEMINI_API_KEY = ""
elevenlabs_api_key = ""

# ---------------------------------------------------------------------------
# Supabase credentials — required for Google Veo and cloud storage
# See SUPABASE_SETUP.md for step-by-step setup instructions.
# ---------------------------------------------------------------------------
# Your Supabase project URL (Project Settings → API → Project URL)
SUPABASE_URL = ""
# Your Supabase anon/public key (Project Settings → API → anon public)
SUPABASE_KEY = ""

# ---------------------------------------------------------------------------
# AI Video Generation — Google Veo via Supabase Edge Function
# ---------------------------------------------------------------------------
# Veo calls are proxied through the "veo-generate" Supabase Edge Function.
# The frontend only needs SUPABASE_URL + SUPABASE_KEY (set above).
#
# Google service-account credentials must be stored as SUPABASE function
# secrets (via `supabase secrets set`), NOT here in frontend secrets.
# See SUPABASE_SETUP.md → Section 6 for details.

# ---------------------------------------------------------------------------
# AI Video Generation — OpenAI Sora
# ---------------------------------------------------------------------------
# The openai_api_key above is reused automatically.  No extra key needed.
